. $NINJA_FUN_DIR/commons

ninja-convert_media() {
  echo -e "Workdir: `pwd`"
  echo -e "docker run -v `pwd`:/tmp/workdir jrottenberg/ffmpeg $*"
  docker run -v `pwd`:/tmp/workdir  jrottenberg/ffmpeg $*
}
